<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/naviagtion"
    app:startDestination="@id/connect_frag">
    <fragment
        android:id="@+id/connect_frag"
        android:name="com.example.kor.ConnectFragment"
        tools:layout="@layout/fragment_connect">
        <action
            android:id="@+id/action_connect_frag_to_drive_frag"
            app:destination="@id/drive_frag" />
    </fragment>
    <fragment
        android:id="@+id/drive_frag"
        android:name="com.example.kor.DriveFragment"
        tools:layout="@layout/fragment_drive"/>
</navigation>